_schema-version: "3.2"
ID: crossfndfioriinbox
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: crossfndfioriinbox-destination-content
  type: com.sap.application.content
  requires:
  - name: crossfndfioriinbox-destination-service
    parameters:
      content-target: true
  - name: crossfndfioriinbox-repo-host
    parameters:
      service-key:
        name: crossfndfioriinbox-repo-host-key
  - name: crossfndfioriinbox-uaa
    parameters:
      service-key:
        name: crossfndfioriinbox-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: crossfndfioriinbox_html_repo_host
          ServiceInstanceName: crossfndfioriinbox-html5-srv
          ServiceKeyName: crossfndfioriinbox-repo-host-key
          sap.cloud.service: crossfndfioriinbox
        - Authentication: OAuth2UserTokenExchange
          Name: crossfndfioriinbox_uaa
          ServiceInstanceName: crossfndfioriinbox-xsuaa-srv
          ServiceKeyName: crossfndfioriinbox-uaa-key
          sap.cloud.service: crossfndfioriinbox
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: crossfndfioriinbox-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: crossfndfioriinbox-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - crossfndfioriinbox.zip
      name: crossfndfioriinbox
      target-path: resources/
- name: crossfndfioriinbox
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: crossfndfioriinbox-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: crossfndfioriinbox-destination-service
    service-plan: lite
- name: crossfndfioriinbox-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: crossfndfioriinbox-xsuaa-srv
    service-plan: application
- name: crossfndfioriinbox-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: crossfndfioriinbox-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
